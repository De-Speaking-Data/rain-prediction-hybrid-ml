# rain-prediction-hybrid-ml
Rain prediction using Logistic Regression, SVM, and a soft-voting hybrid model  with SMOTE for class imbalance and full evaluation (accuracy, precision, recall, F1, ROC-AUC).
